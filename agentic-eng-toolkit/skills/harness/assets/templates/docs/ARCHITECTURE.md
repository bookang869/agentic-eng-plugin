# Architecture

## Directory Structure
```
src/
├── app/               # pages + API routes
├── components/        # UI components
├── types/             # TypeScript type definitions
├── lib/               # utilities + helpers
└── services/          # external API wrappers
```

## Patterns
{Design patterns in use (e.g. Server Components by default, Client Components only where interaction is needed)}

## Data Flow
```
{How data flows (e.g.
user input → Client Component → API Route → external API → response → UI update
)}
```

## State Management
{State management approach (e.g. server state via Server Components, client state via useState/useReducer)}
