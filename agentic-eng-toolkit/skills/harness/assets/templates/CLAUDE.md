# Project: {project name}

## Tech Stack
- {framework (e.g. Next.js 15)}
- {language (e.g. TypeScript strict mode)}
- {styling (e.g. Tailwind CSS)}

## Architecture Rules
- CRITICAL: {absolute rule 1 (e.g. all API logic must be handled only in app/api/ route handlers)}
- CRITICAL: {absolute rule 2 (e.g. never call external APIs directly from client components)}
- {general rule (e.g. components go in components/, types go in types/)}

## Development Process
- CRITICAL: when implementing a new feature, always write tests first, then write the implementation that makes them pass (TDD)
- Commit messages must follow the conventional commits format (feat:, fix:, docs:, refactor:)

## Commands
npm run dev      # dev server
npm run build    # production build
npm run lint     # ESLint
npm run test     # tests
